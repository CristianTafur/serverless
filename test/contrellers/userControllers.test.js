const helper = require('../helper');
// eslint-disable-next-line no-undef
describe('user:', () => {
  // eslint-disable-next-line no-undef
  beforeAll(() => {
    helper.clear();
  });
  // eslint-disable-next-line no-undef
  beforeEach(() => {
    helper.clear();
  });
  // eslint-disable-next-line no-undef
  test('should process steps', async () => {
    // eslint-disable-next-line no-undef
    // expect().toBeCalled();
    // eslint-disable-next-line no-undef
    // expect().toBeCalled();
  });
});
